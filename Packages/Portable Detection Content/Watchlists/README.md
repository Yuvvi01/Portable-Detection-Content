# Watchlists

Watchlists used by ransomware and public threat-intel detection templates. Keep this repository private and review IOC sharing permissions before public release.

Use `PublicThreatIntel/public_ioc_sources.yaml` as a source registry and schema guide. Import actual feed values into the target platform's lookup, watchlist, enrichment table, or threat-intel index.
